-:
	LANG=en_US.UTF-8 && git submodule update --init && bundle install && bundle exec pod install && $(MAKE) -C Dependency/peg-markdown-highlight
update:
	LANG=en_US.UTF-8 && git submodule update && bundle exec pod install

